---
description: Roda todos os portões de qualidade (typecheck + lint + format + testes)
---

Rode `pnpm check` (typecheck + lint + format:check + test). Se algo falhar, corrija a causa
e rode de novo até ficar tudo verde. Reporte um resumo curto do resultado.
