# lib/

Utilitários transversais, sem estado de domínio. Ex.: `utils.ts` (`cn`), helpers de
data, formatação, controle de acesso (RBAC), auditoria, geração de PDF.

Se algo pertence a uma feature específica, vai para `modules/<nome>/`, não aqui.
